# tcp-chat-application
A multithreaded client-server chat application built in C using the Winsock API. The project demonstrates TCP socket programming, multithreading, concurrent client handling, and real-time messaging over a local area network (LAN).
